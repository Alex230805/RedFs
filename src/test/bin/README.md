# Test program binary location

When the test program is compiler it will generate three different binary that can be invokect manually 
to test the formatting functions, the folder functions and navitation system and file functions. 

<br>

Is suggested to use the **test** binary provided in the "src/test" folder that will handle the three test 
sequence automatically.
